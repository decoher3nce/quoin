node_modules/
dist/
dist-ssr/
*.local

# Per-deployment env (PUBLIC_HOST etc.) — never commit; only .env.example is tracked.
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# Local machine config (contains local paths)
.claude/settings.local.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vitest / coverage
coverage/

# Stray non-project state file
data/queue-state.json

# TypeScript incremental build cache
*.tsbuildinfo
